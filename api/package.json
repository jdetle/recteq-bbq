{
  "name": "recteq-lambdas",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "lambda ",
  "main": "index.ts",
  "scripts": {
    "test": "jest",
    "build": "esbuild index.ts --minify --bundle --format=cjs --platform=node --target=node16 --outfile=dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.106",
    "@types/inquirer": "^9.0.0",
    "@types/jest": "^28.1.7",
    "@types/node": "18.7.11",
    "esbuild": "^0.15.12",
    "jest": "^28.1.3",
    "ts-node": "^10.9.1",
    "typescript": "~4.8.4"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.204.0",
    "aws-lambda": "^1.0.7"
  }
}
